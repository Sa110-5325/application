商品一覧
<% @items.each do |item| %>
  <%= link_to item.name, admin_item_path(item) %>
  <%= item.genre.name %>
  <%= item.info %>
  <%= item.price %>
  <% if item.status == true %>
    販売中
  <% else %>
    販売停止中
  <% end %>
<% end %>
<%= link_to "商品登録する", new_admin_item_path %>