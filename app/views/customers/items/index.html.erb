<% @items.each do |item| %>
  <%= link_to attachment_image_tag(item, :image), item_path(item) %>
  商品名<%= item.name %>
  ジャンル<%= item.genre.name %>
  説明<%= item.info %>
  値段<%= item.price %>
<% end %>